<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chores.dzikirapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DzikirApp">
        <activity android:name=".ui.detail.DetailArtikelActivity"></activity>
        <activity android:name=".ui.detail.PetangActivity" />
        <activity android:name=".ui.detail.PagiActivity" />
        <activity android:name=".ui.DzikirPagiDanPetangActivity" />
        <activity android:name=".DzikirSetaipSaatActivity" />
        <activity android:name=".ui.DoaHarianActivity" />
        <activity android:name=".ui.QauliyahActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>